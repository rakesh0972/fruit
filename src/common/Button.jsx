const Button = () => {
	return (
		<div>
			<button
				type="submit"
				className="bg-red text-white py-2 px-4 font-lato font-medium text-xs rounded-md "
			>
				Shop now
			</button>
		</div>
	);
};

export default Button;
